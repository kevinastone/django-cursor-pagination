============
Installation
============

At the command line::

    $ easy_install django-cursor-pagination

Or, if you have virtualenvwrapper installed::

    $ mkvirtualenv django-cursor-pagination
    $ pip install django-cursor-pagination